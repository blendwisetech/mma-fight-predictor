# Copy to secrets.toml for local dev only (never commit secrets.toml).
# On Streamlit Community Cloud: paste the same keys in App settings → Secrets.

THE_ODDS_API_KEY = "your-key-from-the-odds-api-com"
